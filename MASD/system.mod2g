use systemKB as knowledge.

module system {
	%if bel(getRandom(R), R > 0.1), bel(generateNewGarbageLoc(Loc)) then insert(newGarbage(Loc)).
	if bel(not(noReady(X))) 
		then allother.send(newIteration('go')).
}