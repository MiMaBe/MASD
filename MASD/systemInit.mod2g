use systemKB as knowledge.

module systemInit {
	if bel(generateNewGarbageLoc(Loc)) then insert(newGarbage(Loc)).
	if bel(generateNewGarbageLoc(Loc)) then insert(newGarbage(Loc)).
	if bel(generateNewGarbageLoc(Loc)) then insert(newGarbage(Loc)).
	if bel(generateNewGarbageLoc(Loc)) then insert(newGarbage(Loc)).
}